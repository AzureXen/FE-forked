export interface CompanyResponse {
  id: number;
  companyName: string;
}

export interface CompanyResponseAllInfo {
  id: number;
  companyName: string; 
  companyDescription: string;
  location: string;
}